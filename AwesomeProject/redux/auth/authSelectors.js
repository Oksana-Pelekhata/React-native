export const isLogined = state => state.auth.isLoggedIn;

export const id = state => state.auth.uid;

export const name = state => state.auth.username;

export const email = state => state.auth.email;


